# runner-manifest

Workflows for capturing GitHub Actions runner state: installed tools, versions, paths, permissions, and system configuration.

Useful for verifying runner image contents and tracking changes across image updates.

## Usage

fork this repo, then run the `Dump GitHub Runner State` workflow manually from the Actions tab. Output appears in the workflow logs.
